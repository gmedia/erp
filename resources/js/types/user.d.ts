export interface User {
    id: number;
    name: string;
    email: string;
    // Optional avatar URL for user profile images
    avatar?: string;
    email_verified_at?: string;
    created_at: string;
    updated_at: string;
}
