'use client';

import { createEntityCrudPage } from '@/components/common/EntityCrudPage';
import { assetLocationConfig } from '@/utils/entityConfigs';

export default createEntityCrudPage(assetLocationConfig);
