'use client';

import { createEntityCrudPage } from '@/components/common/EntityCrudPage';
import { purchaseOrderConfig } from '@/utils/entityConfigs';

export default createEntityCrudPage(purchaseOrderConfig);
