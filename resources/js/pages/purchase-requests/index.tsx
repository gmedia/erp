'use client';

import { createEntityCrudPage } from '@/components/common/EntityCrudPage';
import { purchaseRequestConfig } from '@/utils/entityConfigs';

export default createEntityCrudPage(purchaseRequestConfig);
