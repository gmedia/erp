'use client';

import { createEntityCrudPage } from '@/components/common/EntityCrudPage';
import { customerConfig } from '@/utils/entityConfigs';

export default createEntityCrudPage(customerConfig);
