'use client';

import { createEntityCrudPage } from '@/components/common/EntityCrudPage';
import { pipelineConfig } from '@/utils/entityConfigs';

export default createEntityCrudPage(pipelineConfig);
