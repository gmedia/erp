'use client';

import { createEntityCrudPage } from '@/components/common/EntityCrudPage';
import { accountMappingConfig } from '@/utils/entityConfigs';

export default createEntityCrudPage(accountMappingConfig);
